# Security

Do not commit passwords, provider credentials, browser cookies, session IDs, API keys, proxy passwords, `.netrc` files, or local `.env` files.

For private security reports, use GitHub's private vulnerability reporting feature when enabled for the repository. For ordinary provider breakage or public bugs, open a regular issue without including credentials or session cookies.
